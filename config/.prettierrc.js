
/**
 * prettier:
 *  - https://prettier.io/docs/en/configuration
*/
/** @type {import('prettier').Config} */
const config = {
  arrowParens: 'avoid',
  printWidth: 100,
  semi: false,
  singleQuote: true,
  trailingComma: 'none'
}

module.exports = config
